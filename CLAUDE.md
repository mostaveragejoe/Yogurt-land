# CLAUDE.md — Yogurt-land

This file provides context for AI assistants (Claude, etc.) working in this repository.

## Project Overview

**Yogurt-land** is a new project in its initial bootstrapping phase. The repository currently contains only the scaffolding (README) and is awaiting technology decisions and source code.

- **Owner:** mostaveragejoe
- **Repository:** `mostaveragejoe/Yogurt-land`
- **Default branch:** `main`
- **License:** Not yet specified

## Repository Structure

```
Yogurt-land/
├── CLAUDE.md          # AI assistant guidance (this file)
└── README.md          # Project overview
```

> This section should be updated as the project grows and new directories/files are added.

## Tech Stack

Not yet established. Update this section when technology choices are made (language, framework, database, etc.).

## Getting Started

No setup steps are required yet. When a build system is introduced, document the following here:

- **Prerequisites** (runtime versions, system dependencies)
- **Install dependencies** (e.g., `npm install`, `pip install -r requirements.txt`)
- **Run the app** (e.g., `npm start`, `python main.py`)

## Common Commands

<!-- Add project commands as they are introduced -->

| Task | Command |
|------|---------|
| *(none yet)* | — |

## Testing

No test framework has been configured yet. When one is added, document:

- How to run the full test suite
- How to run a single test file
- How to run tests in watch mode
- Any required environment variables or test fixtures

## Linting & Formatting

No linting or formatting tools are configured yet. When added, document the commands and any auto-fix options here.

## CI/CD

No continuous integration or deployment pipelines are configured. Update this section when GitHub Actions, etc., are introduced.

## Code Conventions

The following conventions should be followed as the project develops:

- **Commits:** Use clear, descriptive commit messages. Prefer imperative mood (e.g., "Add feature" not "Added feature").
- **Branches:** Feature branches should follow the pattern `feature/<description>` or similar.
- **Pull requests:** Include a summary of changes and any testing done.
- **Code style:** Follow whatever linter/formatter is configured. Consistency is the priority.
- **Files:** Avoid committing secrets, credentials, `.env` files, or large binaries.

## Architecture & Design Decisions

Document important architectural decisions and design patterns here as the project evolves.

## AI Assistant Guidelines

When working in this repository:

1. **Read before writing** — Always read existing files before modifying them.
2. **Minimal changes** — Only make changes that are directly requested or clearly necessary.
3. **Don't over-engineer** — Keep solutions simple. Avoid adding abstractions, error handling, or features that weren't asked for.
4. **Update this file** — When making significant structural changes (new directories, new tools, new workflows), update the relevant sections of this CLAUDE.md.
5. **Respect existing patterns** — Match the style and conventions already present in the codebase.
6. **No secrets** — Never commit credentials, API keys, or sensitive configuration.
