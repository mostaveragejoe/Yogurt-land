<Project Sdk="Microsoft.NET.Sdk">
  <!-- PROTOTYPE - NOT FOR PRODUCTION. Self-contained: deliberately copies the
       primitives instead of referencing src/core, per the prototype rules. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>ModeSwitchSpike</AssemblyName>
    <RootNamespace>ModeSwitchSpike</RootNamespace>
    <Optimize>true</Optimize>
    <ServerGarbageCollection>false</ServerGarbageCollection>
    <ConcurrentGarbageCollection>false</ConcurrentGarbageCollection>
  </PropertyGroup>
</Project>
